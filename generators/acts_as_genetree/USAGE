Description:
    Run this generator to create the migration which will be used to create the tree_encodings table.

Example:
    ./script/generate acts_as_genetree create_tree_encodings

    This will create:
        db/migrate/TIMESTAMP_create_tree_encodings.rb
